/**
 * codegen.ts
 *
 * Punto de extensión reservado para generación de código del frontend
 * (por ejemplo, generar tipos TypeScript a partir del catálogo de precios
 * definido en `index.html`, o sincronizar tipos compartidos con el
 * backend). Actualmente no realiza ninguna transformación: se mantiene
 * como stub para no alterar el comportamiento existente de la app.
 *
 * Ejecutar manualmente con: `npx tsx codegen.ts`
 */

function main(): void {
    console.log('[codegen] No hay generación de código configurada todavía.');
}

main();
