{
  "name": "@planet-producciones/web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sistema VIP de Cotizaciones Planet Producciones 2026 — Frontend",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
