{
  "name": "planet-producciones",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema VIP de Cotizaciones Planet Producciones 2026 (monorepo)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,yellow \"npm run dev --workspace apps/server\" \"npm run dev --workspace apps/web\"",
    "dev:server": "npm run dev --workspace apps/server",
    "dev:web": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace apps/server && npm run build --workspace apps/web",
    "build:server": "npm run build --workspace apps/server",
    "build:web": "npm run build --workspace apps/web",
    "start": "npm run start --workspace apps/server",
    "typecheck": "npm run typecheck --workspace apps/server && npm run typecheck --workspace apps/web"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
